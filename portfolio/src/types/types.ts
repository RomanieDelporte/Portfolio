export interface CardData {
    title: string;
    description: string;
    topics: string[];
    url: string;
}

export interface GHRepoCardData {
    name: string;
    description: string;
    topics: string[];
    clone_url: string;
}